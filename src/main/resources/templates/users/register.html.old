<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

	<head th:replace="layout :: site-head">
		<title>Queuefir - Registration</title>
	</head>

	<body>
		<header th:replace="layout :: site-header" />
			<form id='register-form' action="" method='post'>
				<input type="text" placeholder="Username" required>
				<p>
				<input type="fullname" placeholder="Full Name" required>
				<p>
				<input type="zipcode" placeholder="Zip Code" required>
				<p>
				<input type="password" placeholder="Password" required>
				<p>
				<input type="password" placeholder="Confirm Password" required>
				<p>
				<form:radiobutton path="hasKefir" value="true"/>
					<spring:message code="label.roundYes"/>
					<form:radiobutton path="hasKefir" value="false"/>
						<spring:message code="label.roundNo"/>
						<button type='submit'>Register</button>
				<p>
				<a href="login.html" th:href="@{/users/login}">Already a Member ? Sign in Now.</a>
			</form>

			<footer th:replace="layout :: site-footer" />
	</body>


</html>
